import React from "react";
import { Button } from "@material-ui/core";

interface Props { }

function Product(props: { myFunc: () => void }) {
  return (
    <div>
      <h1>Product page here</h1>
    </div>
  );
}

export default Product;

